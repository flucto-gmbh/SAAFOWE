# fetch_msbdata.py

1. List available motion sensor boxes avialable either via reverse ssh
connections or the local network.
2. list available data time intervals for a chosen box
3. download selected data from a defined box
4. aggregate selected data into new files / a new file
